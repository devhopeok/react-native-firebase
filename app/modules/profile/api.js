import { auth, database, provider } from "../../config/firebase";

//var authData = auth.currentUser.uid;
/*
if (authData) {
alert('we out here');
  console.log("Authenticated user with uid:", authData.uid);
}

export function getUserId(authData) {
    alert('we out here');
    console.log("Authenticated user with uid:", authData.uid);
}
*/